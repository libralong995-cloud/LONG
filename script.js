document.querySelectorAll('.rail').forEach(rail=>{let down=false,x=0,s=0;rail.addEventListener('wheel',e=>{if(Math.abs(e.deltaY)>Math.abs(e.deltaX)){e.preventDefault();rail.scrollLeft+=e.deltaY}},{passive:false});rail.addEventListener('pointerdown',e=>{down=true;x=e.clientX;s=rail.scrollLeft;rail.setPointerCapture(e.pointerId)});rail.addEventListener('pointermove',e=>{if(down)rail.scrollLeft=s-(e.clientX-x)});rail.addEventListener('pointerup',()=>down=false);rail.addEventListener('pointercancel',()=>down=false)});
